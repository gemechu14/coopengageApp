org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
# Kotlin incremental compile fails on Windows when pub cache (C:) and project (D:) differ — KT RelocatableFileToPathConverter
kotlin.incremental=false
android.useAndroidX=true
android.enableJetifier=true

# Use system-installed Java 11

# Add these new Vulkan/OpenGL workarounds
android.enableVulkanValidation=false
android.vulkan.available=false  # Force disable Vulkan
android.gradle.parallel=true    # Improve build performance
android.gradle.caching=true